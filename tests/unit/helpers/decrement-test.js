import { decrement } from 'migrate-app/helpers/decrement';
import { module, test } from 'qunit';

module('Unit | Helper | decrement');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = decrement([42]);
  assert.ok(result);
});
